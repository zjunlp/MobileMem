"""External capabilities (L1): adapters over the powers the pipeline uses.

llm/ (text) · images.py (image generation) · faces.py (FaceEngine + orientation).
These import only config + stdlib/third-party; never a generator or the domain model.
"""
