# `generation/annual_events/` — Annual Events

Generates a year of life events for each persona.

| Module | Responsibility |
|--------|----------------|
| `generator.py` | Drives event generation per persona |
| `parse.py` | Parses and validates the LLM event output |
| `names.py` | Event naming helpers |
