# `generation/document/` — Document Images

Generates document-style images for events.

| Module | Responsibility |
|--------|----------------|
| `generator.py` | Orchestrates document generation per event |
| `content.py` | Produces the document content via the LLM |
| `templates.py` | Document HTML templates |
